<UserControl x:Class="DeveloperBrowser.App.NetworkInspectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DeveloperBrowser.App"
             Background="{StaticResource CanvasBrush}">
    <UserControl.Resources>
        <Style TargetType="ListBox"><Setter Property="Background" Value="Transparent" /><Setter Property="BorderThickness" Value="0" /><Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" /></Style>
        <Style TargetType="ListBoxItem"><Setter Property="Padding" Value="0" /><Setter Property="Margin" Value="0" /><Setter Property="Background" Value="Transparent" /><Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" /><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem"><Border x:Name="Row" Background="{TemplateBinding Background}" Padding="8,7"><ContentPresenter /></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Row" Property="Background" Value="#FF292D37" /></Trigger><Trigger Property="IsSelected" Value="True"><Setter TargetName="Row" Property="Background" Value="#FF1E3A5F" /></Trigger><DataTrigger Binding="{Binding IsFailed}" Value="True"><Setter TargetName="Row" Property="Background" Value="#FF40252B" /></DataTrigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="Padding" Value="12,7" />
            <Setter Property="Margin" Value="0,0,8,6" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Tab"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Tab" Property="Background" Value="#FF292E38" />
                                <Setter TargetName="Tab" Property="BorderBrush" Value="#FF596579" />
                                <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
                                <Setter TargetName="Tab" Property="Background" Value="#FF193753" />
                                <Setter TargetName="Tab" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NetworkDetailTabs" TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <WrapPanel Grid.Row="0" IsItemsHost="True" Margin="0,0,0,8" />
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              Grid.Row="1"
                                              ContentSource="SelectedContent" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GhostButton" TargetType="Button"><Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" /><Setter Property="Background" Value="{StaticResource SurfaceBrush}" /><Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" /><Setter Property="BorderThickness" Value="1" /><Setter Property="Padding" Value="10,6" /><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7"><ContentPresenter Margin="{TemplateBinding Padding}" /></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBorder" Property="Background" Value="#FF292D37" /><Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#FF53627A" /></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style>
        <Style x:Key="NetworkCloseButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
            <Setter Property="Width" Value="32" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
            <Setter Property="ToolTip" Value="Close network inspector" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#FFFFC9C9" />
                    <Setter Property="Background" Value="#FF3B272D" />
                    <Setter Property="BorderBrush" Value="#FF85505A" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="NetworkFilterItem" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemSurface" Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemSurface" Property="Background" Value="#FF2C3545" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemSurface" Property="Background" Value="#FF193753" />
                                <Setter Property="Foreground" Value="#FFF5FAFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NetworkFilterBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="Background" Value="#FF202633" />
            <Setter Property="BorderBrush" Value="#FF465A78" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Height" Value="34" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource NetworkFilterItem}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Focusable="False"
                                          Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="FilterSurface"
                                                Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                CornerRadius="7">
                                            <Grid>
                                                <ContentPresenter Margin="10,0,30,0"
                                                                  VerticalAlignment="Center"
                                                                  Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                  ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}" />
                                                <Path HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,11,0"
                                                      Data="M 0 0 L 4 4 L 8 0" Stroke="#FFAAB8CA" StrokeThickness="1.5" StrokeLineJoin="Round" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="FilterSurface" Property="Background" Value="#FF293243" />
                                                <Setter TargetName="FilterSurface" Property="BorderBrush" Value="#FF617492" />
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="FilterSurface" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PlacementTarget="{Binding ElementName=ToggleButton}"
                                   PopupAnimation="Slide">
                                <Border MinWidth="{Binding ActualWidth, ElementName=ToggleButton}"
                                        Margin="0,4,0,0"
                                        Padding="3"
                                        Background="#FF202633"
                                        BorderBrush="#FF536987"
                                        BorderThickness="1"
                                        CornerRadius="7">
                                    <ScrollViewer CanContentScroll="True" MaxHeight="280" VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Margin="18,12"><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <WrapPanel VerticalAlignment="Center">
                <TextBlock Text="NETWORK" Foreground="{StaticResource PrimaryTextBrush}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,7,14,7" />
                <ComboBox x:Name="FilterBox" Style="{StaticResource NetworkFilterBox}" Width="132" Margin="0,0,8,0" SelectedIndex="0" SelectionChanged="FilterBox_SelectionChanged"><ComboBoxItem Content="All" /><ComboBoxItem Content="XHR / Fetch" /><ComboBoxItem Content="Document" /><ComboBoxItem Content="JS" /><ComboBoxItem Content="CSS" /><ComboBoxItem Content="Images" /><ComboBoxItem Content="Failed" /></ComboBox>
                <TextBox x:Name="SearchBox" Width="220" Margin="0,0,10,0" Style="{StaticResource SearchInput}" Tag="Search URL…" VerticalContentAlignment="Center" TextChanged="SearchBox_TextChanged" Text="" />
                <TextBlock x:Name="RequestCountText" Foreground="{StaticResource MutedTextBrush}" FontSize="12" VerticalAlignment="Center" Margin="0,7,12,7" />
                <Button Style="{StaticResource GhostButton}" Margin="0,0,8,0" Click="Clear_Click"><StackPanel Orientation="Horizontal"><Viewbox Width="14" Height="14"><Path Data="M6,7V19H18V7 M4,5H20 M9,5V3H15V5 M10,10V16 M14,10V16" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round" /></Viewbox><TextBlock Text="Clear" Margin="6,0,0,0" VerticalAlignment="Center" /></StackPanel></Button>
                <Button x:Name="DockBottomButton" Style="{StaticResource GhostButton}" Margin="0,0,8,0" Click="DockBottom_Click" ToolTip="Move Network inspector to the bottom"><StackPanel Orientation="Horizontal"><Viewbox Width="14" Height="14"><Path Data="M11,4V15L7,11L5.5,12.5L12,19L18.5,12.5L17,11L13,15V4H11Z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" /></Viewbox><TextBlock Text="Move to bottom" Margin="6,0,0,0" VerticalAlignment="Center" /></StackPanel></Button>
                <Button x:Name="DockRightButton" Style="{StaticResource GhostButton}" Margin="0,0,8,0" Click="DockRight_Click" ToolTip="Dock Network inspector on the right"><StackPanel Orientation="Horizontal"><Viewbox Width="14" Height="14"><Path Data="M4,11V13H15L11,17L12.5,18.5L19,12L12.5,5.5L11,7L15,11H4Z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" /></Viewbox><TextBlock Text="Move to right" Margin="6,0,0,0" VerticalAlignment="Center" /></StackPanel></Button>
            </WrapPanel>
            <Button Grid.Column="1" Style="{StaticResource NetworkCloseButton}" Click="Close_Click" HorizontalAlignment="Right" VerticalAlignment="Top"><Viewbox Width="13" Height="13"><Path Data="M5,5 L19,19 M19,5 L5,19" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round" /></Viewbox></Button>
        </Grid>
        <Grid x:Name="InspectorContentGrid" Grid.Row="1"><Grid.ColumnDefinitions><ColumnDefinition Width="58*" /><ColumnDefinition Width="10" /><ColumnDefinition Width="42*" /></Grid.ColumnDefinitions>
            <Border x:Name="RequestListPanel" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="10">
                <Grid><Grid.RowDefinitions><RowDefinition Height="32" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Grid Margin="8,0" Background="#FF15171D"><Grid.ColumnDefinitions><ColumnDefinition Width="66" /><ColumnDefinition Width="*" /><ColumnDefinition Width="58" /><ColumnDefinition Width="70" /><ColumnDefinition Width="130" /><ColumnDefinition Width="130" /></Grid.ColumnDefinitions><TextBlock Text="Method" Foreground="{StaticResource MutedTextBrush}" FontSize="11" VerticalAlignment="Center" /><TextBlock Grid.Column="1" Text="URL" Foreground="{StaticResource MutedTextBrush}" FontSize="11" VerticalAlignment="Center" /><TextBlock Grid.Column="2" Text="Status" Foreground="{StaticResource MutedTextBrush}" FontSize="11" VerticalAlignment="Center" /><TextBlock Grid.Column="3" Text="Time" Foreground="{StaticResource MutedTextBrush}" FontSize="11" VerticalAlignment="Center" /><TextBlock Grid.Column="4" Text="Request type" Foreground="{StaticResource MutedTextBrush}" FontSize="11" VerticalAlignment="Center" /><TextBlock Grid.Column="5" Text="Response type" Foreground="{StaticResource MutedTextBrush}" FontSize="11" VerticalAlignment="Center" /></Grid>
                    <ListBox x:Name="RequestList" Grid.Row="1" ItemsSource="{Binding Requests}" SelectionChanged="RequestList_SelectionChanged"><ListBox.ItemTemplate><DataTemplate><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="66" /><ColumnDefinition Width="*" /><ColumnDefinition Width="58" /><ColumnDefinition Width="70" /><ColumnDefinition Width="130" /><ColumnDefinition Width="130" /></Grid.ColumnDefinitions><TextBlock Text="{Binding Method}" Foreground="{StaticResource AccentBrush}" FontFamily="Cascadia Mono" FontWeight="SemiBold" FontSize="11" /><TextBlock Grid.Column="1" Text="{Binding Url}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Url}" /><TextBlock Grid.Column="2" Text="{Binding StatusText}" Foreground="{StaticResource PrimaryTextBrush}" /><TextBlock Grid.Column="3" Text="{Binding DurationText}" Foreground="{StaticResource MutedTextBrush}" /><TextBlock Grid.Column="4" Text="{Binding RequestContentType}" TextTrimming="CharacterEllipsis" Foreground="{StaticResource MutedTextBrush}" /><TextBlock Grid.Column="5" Text="{Binding ResponseContentType}" TextTrimming="CharacterEllipsis" Foreground="{StaticResource MutedTextBrush}" /></Grid></DataTemplate></ListBox.ItemTemplate></ListBox>
                </Grid>
            </Border>
            <GridSplitter x:Name="InspectorDetailSplitter" Grid.Column="1" Width="10" Background="Transparent" ResizeDirection="Columns" ShowsPreview="True" />
            <Border x:Name="RequestDetailPanel" Grid.Column="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="14"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock x:Name="DetailMethodText" Text="Select a request" Foreground="{StaticResource PrimaryTextBrush}" FontFamily="Cascadia Mono" FontWeight="SemiBold" /><TextBlock x:Name="DetailUrlText" Foreground="{StaticResource MutedTextBrush}" FontSize="11" TextTrimming="CharacterEllipsis" MaxWidth="430" /></StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal"><Button x:Name="JwtInspectButton" Content="Inspect JWT" Style="{StaticResource GhostButton}" Click="InspectJwt_Click" IsEnabled="False" ToolTip="Decode the JWT locally; the token is not sent anywhere." /><Button x:Name="OpenInRestButton" Content="Open in REST Client" Style="{StaticResource PrimaryButton}" Click="OpenInRest_Click" IsEnabled="False" Margin="8,0,0,0" /></StackPanel></Grid>
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0"><Border x:Name="DetailStatusBadge" Background="#FF263238" CornerRadius="7" Padding="8,4"><TextBlock x:Name="DetailStatusText" Text="No request selected" Foreground="{StaticResource PrimaryTextBrush}" FontSize="12" /></Border><TextBlock x:Name="DetailTimingText" Foreground="{StaticResource MutedTextBrush}" FontSize="12" VerticalAlignment="Center" Margin="10,0,0,0" /></StackPanel>
                <TabControl Grid.Row="2" Margin="0,12,0,0" Style="{StaticResource NetworkDetailTabs}"><TabItem Header="Request headers"><TextBox x:Name="RequestHeadersBox" Margin="0,10,0,0" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Cascadia Mono" FontSize="12" /></TabItem><TabItem Header="Response headers"><TextBox x:Name="ResponseHeadersBox" Margin="0,10,0,0" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Cascadia Mono" FontSize="12" /></TabItem><TabItem Header="Query"><TextBox x:Name="QueryBox" Margin="0,10,0,0" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Cascadia Mono" FontSize="12" /></TabItem><TabItem Header="Request body"><TextBox x:Name="RequestBodyBox" Margin="0,10,0,0" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Cascadia Mono" FontSize="12" /></TabItem><TabItem Header="Response body"><local:StructuredDataViewer x:Name="ResponseDataViewer" Margin="0,10,0,0" /></TabItem><TabItem Header="Cookies"><TextBox x:Name="CookiesBox" Margin="0,10,0,0" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Cascadia Mono" FontSize="12" /></TabItem><TabItem Header="Timing"><TextBox x:Name="TimingBox" Margin="0,10,0,0" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Cascadia Mono" FontSize="12" /></TabItem><TabItem Header="CORS / CSP"><TextBox x:Name="PolicyAnalysisBox" Margin="0,10,0,0" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Segoe UI" FontSize="12" Padding="12,10" /></TabItem><TabItem x:Name="JwtDetailTab" Header="JWT"><ScrollViewer Margin="0,10,0,0" VerticalScrollBarVisibility="Auto"><StackPanel><Border x:Name="JwtTimeStatusBadge" Background="#FF263238" CornerRadius="7" Padding="9,5" HorizontalAlignment="Left"><TextBlock x:Name="JwtTimeStatusText" Text="JWT not inspected" Foreground="{StaticResource PrimaryTextBrush}" FontSize="12" FontWeight="SemiBold" /></Border><TextBlock x:Name="JwtSignatureText" Text="Signature: not validated" Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,14" TextWrapping="Wrap" /><TextBlock Text="Decoded locally from the captured Authorization header. The token is never sent by this inspector, and decoding does not validate its signature." Foreground="{StaticResource MutedTextBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,14" /><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions><TextBlock Text="Issuer" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,14,8" /><TextBlock x:Name="JwtIssuerText" Grid.Column="1" Foreground="{StaticResource PrimaryTextBrush}" TextWrapping="Wrap" Margin="0,0,0,8" /><TextBlock Grid.Row="1" Text="Audience" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,14,8" /><TextBlock x:Name="JwtAudienceText" Grid.Row="1" Grid.Column="1" Foreground="{StaticResource PrimaryTextBrush}" TextWrapping="Wrap" Margin="0,0,0,8" /><TextBlock Grid.Row="2" Text="Subject" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,14,8" /><TextBlock x:Name="JwtSubjectText" Grid.Row="2" Grid.Column="1" Foreground="{StaticResource PrimaryTextBrush}" TextWrapping="Wrap" Margin="0,0,0,8" /><TextBlock Grid.Row="3" Text="Scopes" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,14,8" /><TextBlock x:Name="JwtScopesText" Grid.Row="3" Grid.Column="1" Foreground="{StaticResource PrimaryTextBrush}" TextWrapping="Wrap" Margin="0,0,0,8" /><TextBlock Grid.Row="4" Text="Roles" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,14,8" /><TextBlock x:Name="JwtRolesText" Grid.Row="4" Grid.Column="1" Foreground="{StaticResource PrimaryTextBrush}" TextWrapping="Wrap" Margin="0,0,0,8" /><TextBlock Grid.Row="5" Text="Issued at" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,14,8" /><TextBlock x:Name="JwtIssuedAtText" Grid.Row="5" Grid.Column="1" Foreground="{StaticResource PrimaryTextBrush}" TextWrapping="Wrap" Margin="0,0,0,8" /><TextBlock Grid.Row="6" Text="Not before" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,14,8" /><TextBlock x:Name="JwtNotBeforeText" Grid.Row="6" Grid.Column="1" Foreground="{StaticResource PrimaryTextBrush}" TextWrapping="Wrap" Margin="0,0,0,8" /><TextBlock Grid.Row="7" Text="Expires" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,14,8" /><TextBlock x:Name="JwtExpiresText" Grid.Row="7" Grid.Column="1" Foreground="{StaticResource PrimaryTextBrush}" TextWrapping="Wrap" Margin="0,0,0,8" /><TextBlock Grid.Row="8" Text="Time remaining" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,14,0" /><TextBlock x:Name="JwtRemainingText" Grid.Row="8" Grid.Column="1" Foreground="{StaticResource PrimaryTextBrush}" TextWrapping="Wrap" /></Grid><TextBlock Text="Header" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="SemiBold" Margin="0,18,0,6" /><TextBox x:Name="JwtHeaderBox" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Cascadia Mono" FontSize="12" MinHeight="90" /><TextBlock Text="Payload" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="SemiBold" Margin="0,14,0,6" /><TextBox x:Name="JwtPayloadBox" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Cascadia Mono" FontSize="12" MinHeight="160" /></StackPanel></ScrollViewer></TabItem></TabControl>
            </Grid></Border>
        </Grid>
    </Grid>
</UserControl>
