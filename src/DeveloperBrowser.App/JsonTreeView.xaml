<UserControl x:Class="DeveloperBrowser.App.JsonTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DeveloperBrowser.App"
             Background="Transparent">
    <UserControl.Resources>
        <HierarchicalDataTemplate DataType="{x:Type local:JsonTreeNode}" ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal" Margin="2,4,0,4">
                <TextBlock Text="{Binding Key}" Foreground="{StaticResource PrimaryTextBrush}" FontFamily="Cascadia Mono" FontSize="12" />
                <TextBlock Text="{Binding Separator}" Foreground="{StaticResource MutedTextBrush}" FontFamily="Cascadia Mono" FontSize="12" />
                <TextBlock Text="{Binding Value}" Foreground="{Binding ValueBrush}" FontFamily="Cascadia Mono" FontSize="12" TextTrimming="CharacterEllipsis" MaxWidth="420" />
            </StackPanel>
        </HierarchicalDataTemplate>
        <Style x:Key="JsonExpander" TargetType="ToggleButton">
            <Setter Property="Width" Value="18" /><Setter Property="Height" Value="18" /><Setter Property="Margin" Value="0,2,5,0" />
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ToggleButton"><Border x:Name="GlyphBorder" Background="#FF292D37" CornerRadius="4"><Grid><TextBlock x:Name="Plus" Text="+" Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" /><TextBlock x:Name="Minus" Text="−" Foreground="{StaticResource PrimaryTextBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" Visibility="Collapsed" /></Grid></Border><ControlTemplate.Triggers><Trigger Property="IsChecked" Value="True"><Setter TargetName="Plus" Property="Visibility" Value="Collapsed" /><Setter TargetName="Minus" Property="Visibility" Value="Visible" /><Setter TargetName="GlyphBorder" Property="Background" Value="#FF1E3A5F" /></Trigger><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="GlyphBorder" Property="Background" Value="#FF353B48" /></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="IsExpanded" Value="True" />
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TreeViewItem"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="23" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><ToggleButton x:Name="Expander" Grid.Column="0" Style="{StaticResource JsonExpander}" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" /><ContentPresenter Grid.Column="1" ContentSource="Header" /></Grid><ItemsPresenter x:Name="ItemsHost" Grid.Row="1" Grid.Column="1" Margin="23,0,0,0" /></Grid><ControlTemplate.Triggers><Trigger Property="HasItems" Value="False"><Setter TargetName="Expander" Property="Visibility" Value="Hidden" /></Trigger><Trigger Property="IsExpanded" Value="False"><Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" /></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <TextBlock x:Name="EmptyText" Text="Valid JSON will appear as an expandable tree." Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="4,12" VerticalAlignment="Top" />
        <TreeView x:Name="Tree" Background="Transparent" BorderThickness="0" Visibility="Collapsed" />
    </Grid>
</UserControl>
