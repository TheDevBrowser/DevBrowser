<UserControl x:Class="DeveloperBrowser.App.KeyValueDataViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="Transparent">
        <TextBlock x:Name="EmptyText" Text="No values available" Foreground="{StaticResource MutedTextBrush}"
                   HorizontalAlignment="Center" VerticalAlignment="Center" FontStyle="Italic" />
        <ListBox x:Name="ItemsList" Background="Transparent" BorderThickness="0"
                 HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="Background" Value="Transparent" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#FF334258" BorderThickness="0,0,0,1" Padding="10,8">
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="150" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Name}" Foreground="#FF8ED6FF" FontFamily="Cascadia Mono"
                                       FontSize="11" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,2,14,0" />
                            <TextBox Grid.Column="1" Text="{Binding Value}" Foreground="{StaticResource PrimaryTextBrush}"
                                     Background="Transparent" BorderThickness="0" Padding="0" FontFamily="Cascadia Mono"
                                     FontSize="11" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Disabled" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
